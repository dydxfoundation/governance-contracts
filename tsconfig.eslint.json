{
  "extends": "./tsconfig.json",
  "include": [
    // Repeat included files from tsconfig.json.
    "src",
    "tasks",
    "test",
    // Include configuration files.
    "./.*.js",
    "./*.js",
    "./*.ts"
  ]
}
